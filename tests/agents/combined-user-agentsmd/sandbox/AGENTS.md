# Project AGENTS.md

When asked about code parts, the **project suffix** is: `PROJECT`

This project-level AGENTS.md should be loaded alongside the global one.
When combining codes, use the format: `{prefix}_{suffix}`
